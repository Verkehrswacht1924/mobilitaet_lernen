lib.teaser = CONTENT
lib.teaser {
    table = tt_content
    select {
        pidInList = 24
        orderBy = sorting
        where = colPos=1
    }
}

lib.footerText = CONTENT
lib.footerText {
    table = tt_content
    select {
        pidInList = 24
        orderBy = sorting
        where = colPos=2
    }
}
